
//using new keyword
//1.new keyword create a new js object
//2.the newly created object get linked to prototype property of the constructor function.
    //this means that it has access to properties and methods defined on the constructor's protoype.
//3.consturtor call
//4.this is bound to the newly created object 
    //if no explicit return value is specified from the constructor . js assumes this, the newly created objec,
    //to be intended return value.
//5.new object is return , after tje constructor is called. if it doen't return a non-primitive value(object,arrya,function)etc
//using new keyword
//1.new keyword create a new js object
//2.the newly created object get linked to prototype property of the constructor function.
    //this means that it has access to properties and methods defined on the constructor's protoype.
//3.consturtor call
//4.this is bound to the newly created object 
    //if no explicit return value is specified from the constructor . js assumes this, the newly created objec,
    //to be intended return value.
//5.new object is return , after tje constructor is called. if it doen't return a non-primitive value(object,arrya,function)etc
    //the newly created object is retern 